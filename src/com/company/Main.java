package com.company;

import java.util.concurrent.Semaphore;

public class Main {

    public static void main(String[] args) {
        int readcount, writecount = 0; // two static vars shared among all threads/classes.
//        SEM  wmutex, rmutex = 1;    // semaphores to gain mutual exclusion on counters
//        SEM  wanting = 1;    // writers waiting to get in
//        SEM  writerswaiting  = 0;  // readers block here if writers are waiting
//
//        Semaphore semaphore = new Semaphore(numberOfPermits, true);
//        ProcessThread p[] = new ProcessThread[numberOfProcesses];

    }
}
